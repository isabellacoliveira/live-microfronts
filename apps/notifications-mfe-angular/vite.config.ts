import { defineConfig } from 'vite';

// Configuração mínima do Vite para o MFE Angular.
// Responsabilidade: servir o exemplo sem adicionar complexidade desnecessária.

export default defineConfig({
  server: {
    port: 5003,
    strictPort: true,
  },
});
